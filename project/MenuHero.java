import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class MenuHero here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class MenuHero extends Actor
{
    private int yOffset = 0;
    private int direction = 1;
    
    public MenuHero(){
        setImage("mainhero.png");
    }
    public void act() {
    //yOffset += direction;
    //if (yOffset > 5 || yOffset < -5) direction *= -1;
    //setLocation(getX(), 325 + yOffset); 
    }
}

